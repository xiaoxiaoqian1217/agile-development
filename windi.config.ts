import { defineConfig } from 'vite-plugin-windicss';

export default defineConfig({
  safelist: ['bg-gray-100', 'bg-gray-500', 'bg-sky-500', 'bg-amber-200'],
  theme: {
    // colors: {
    //   pr
    //   // 在这里配置你的调色板
    // },
  },
});
